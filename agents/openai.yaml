interface:
  display_name: "feishu_link_Bot"
  short_description: "把 OpenClaw 正式接入飞书机器人"
  default_prompt: "Use $feishu-link-bot to connect OpenClaw to a Feishu self-built bot, publish the online version, and verify end-to-end messaging."

policy:
  allow_implicit_invocation: true
